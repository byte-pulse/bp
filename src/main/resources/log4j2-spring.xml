<?xml version="1.0" encoding="utf-8" ?>
<Configuration monitorInterval="30" status="off" xmlns="https://logging.apache.org/xml/ns"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-config-2.xsd">

    <Properties>
        <Property name="LOG_DIR">.logs/</Property>
    </Properties>


    <!-- 日志处理 -->
    <Appenders>
        <!-- 控制台输出 -->
        <Console name="console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%green{%d{yyyy-MM-dd HH:mm:ss}} [%magenta{%thread}] %magenta{[%pid]} [%highlight{%-5p}] %cyan{%logger{1.}}⤵%n%msg%n%ex%n"/>
        </Console>
        <!-- 输出到文件 -->
        <RollingFile name="rollingFile"
                     fileName="${LOG_DIR}/${spring:spring.application.name}/${date:yyyy-MM-dd}/error.log"
                     filePattern="${LOG_DIR}/${spring:spring.application.name}/${date:yyyy-MM-dd}/error-%i.log">
            <!-- 过滤级别 -->
            <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="deny"/>
            <!-- 消息格式 -->
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] [%pid] [%-5level] : %msg%n %ex"/>
            <Policies>
                <!-- 单文件最大大小 -->
                <SizeBasedTriggeringPolicy size="5MB"/>
            </Policies>
            <DefaultRolloverStrategy max="20"/>
        </RollingFile>

        <Async name="asyncRollingFile">
            <AppenderRef ref="rollingFile"/>
        </Async>
        <Async name="asyncConsole">
            <AppenderRef ref="console"/>
        </Async>
    </Appenders>

    <Loggers>
        <SpringProfile name="dev,test">
            <Root level="error">
                <AppenderRef ref="console"/>
            </Root>
            <Logger name="cloud.bytepulse" level="debug" additivity="false">
                <AppenderRef ref="asyncConsole"/>
                <AppenderRef ref="asyncRollingFile" level="error"/>
            </Logger>
            <Logger name="org.springframework" level="info" additivity="false">
                <AppenderRef ref="asyncConsole"/>
                <AppenderRef ref="asyncRollingFile" level="error"/>
            </Logger>
        </SpringProfile>


        <SpringProfile name="prod">
            <Root level="error">
                <AppenderRef ref="console"/>
                <AppenderRef ref="asyncRollingFile"/>
            </Root>
        </SpringProfile>
    </Loggers>
</Configuration>